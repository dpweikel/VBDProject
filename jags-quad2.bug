model {
  
  for (i in 1:N) {
    Y[i] ~ dnorm(mu[i], tau)T(0,)
    mu[i] <- qd*(T[i]-T0)*(T[i]-Tm)*((T[i]>T0))*((T[i]<Tm))
  }

  Tm  ~ dunif(22,45)
  T0 ~ dunif(0,21)
  qd  ~ dgamma(1,100)
  sigma<-1/tau
  tau ~ dgamma(0.0001, 0.0001)

}
