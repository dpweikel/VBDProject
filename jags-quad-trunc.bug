model {
  
  for (i in 1:N) {
    Y[i] ~ dnorm(mu[i], tau)T(0,)
    mu.temp[i] <- inter-n.slope*T[i]+qd*T[i]^2
    mu[i]<- 0*(mu.temp[i]<0) + mu.temp[i]*(mu.temp[i]>0)
  }

  inter ~ dgamma(4,1)
  n.slope ~ dgamma(1, 3)
  qd  ~ dgamma(6, 1000)
  sigma<-1/tau
  tau ~ dnorm(1000, 1/500)

}
